import type { ImportMap } from 'payload';

// Empty import map is enough for the default admin. This file can be extended
// if custom admin components are added later.
const importMap: ImportMap = {};

export default importMap;
